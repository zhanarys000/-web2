{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "1. Clone the repository\r 2. Install dependencies with `npm install`\r 3. Create a `.env` file and provide your OpenWeather API key",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.5",
    "dotenv": "^16.3.2",
    "express": "^4.18.2",
    "leaflet": "^1.9.4"
  }
}
